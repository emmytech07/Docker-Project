{
  "name": "cypress-e2e-tests",
  "version": "1.0.0",
  "description": "Cypress E2E tests with Docker and GitHub Actions",
  "scripts": {
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^15.9.0",
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1"
  }
}
